import 'package:flutter/material.dart';

class LoadFood extends StatefulWidget {
  @override
  _LoadFoodState createState() => _LoadFoodState();
}

class _LoadFoodState extends State<LoadFood> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(''),
      ),
      body: Center(
        child: Container(
          child: Text(''),
        ),
      ),
    );
  }
}
